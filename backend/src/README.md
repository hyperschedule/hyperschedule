To run the backend for development, run

```
pnpm start-db
pnpm serve
```

When you are done, run `pnpm stop-db` to stop it. You can also restart the database
with `pnpm restart-db` to load fresh sample data. 
